/* firebaseUtils.js - utility functions for updating/modifying Firebase database
  * Author: Allie Zhao
  * Created: 9/3/2023
  */

import { db } from '../firebase.js';

export function addNewMap(mapData, width, height, ownerId, frozen) {
  // check if owner is already at max number of maps, deny if at/over max
  
  // generate new map ID
  
  // add width, height, mapData, ownerId, frozen to map entry
  
  // add mapId to user's list of owned maps
}
